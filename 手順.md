# Web版 Rogue Clone II 無料Web公開手順書 (手順.md)

本ドキュメントでは、本プロジェクトを **完全無料** でインターネット上に公開し、PCやスマホのブラウザから誰でも遊べるようにする手順を解説します。

---

## 最重要：GitHub Pages の有効化設定（初回のみ・1分）

GitHub Actions から GitHub Pages へデプロイするには、**リポジトリの初期設定で「GitHub Actions」を選択する** 必要があります。（※これを行わないと `Error: Get Pages site failed (404 Not Found)` となります）

### 手順：
1. ブラウザで GitHub リポジトリを開きます：
   👉 **[https://github.com/jam648/web-rogue-clone2](https://github.com/jam648/web-rogue-clone2)**
2. 上部メニューの **「Settings（設定）」** をクリックします。
3. 左サイドバーの **「Pages」**（Code and automation 内）をクリックします。
4. **「Build and deployment」** にある **「Source」** のドロップダウンをクリックします。
5. **`GitHub Actions`** を選択します。（※「Deploy from a branch」から切り替えます）
6. 設定は完了です！

---

## 1. GitHub へのコミット & プッシュ

ターミナルで以下のコマンドを実行し、最新のワークフローとコードをプッシュします。

```bash
# 1. 変更ファイルをステージング
git add .

# 2. コミットを作成
git commit -m "fix: update GitHub Actions workflow for Pages"

# 3. リモートリポジトリへプッシュ
git push origin main
```
*(※ ブランチ名が `master` の場合は `git push origin master`)*

---

## 2. 自動デプロイの確認

1. リポジトリの **「Actions」** タブを開きます。
2. `Deploy to GitHub Pages` ワークフローが自動で実行され、緑色のチェックマーク（成功）になります。
3. デプロイ完了後、以下の URL でゲームが遊べるようになります：
   👉 **`https://jam648.github.io/web-rogue-clone2/`**

---

## 3. その他の無料公開方法 (Cloudflare Pages 等)

### Cloudflare Pages での公開（超高速・無制限・カスタムドメイン対応）
1. **[Cloudflare ダッシュボード](https://dash.cloudflare.com/)** にログイン。
2. **Workers & Pages** → **Create application** → **Pages** → **Connect to Git** を選択。
3. `web-rogue-clone2` を選択し、ビルド設定を入力：
   * **Framework preset**: `Vite`
   * **Root directory**: `web`
   * **Build command**: `npm run build`
   * **Build output directory**: `dist`
4. **Save and Deploy** をクリック。
